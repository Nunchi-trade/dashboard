import React from "react";

export default function Footer() {
  return (
    <footer className="py-8">
      <p className="text-xs text-desc leading-relaxed">
        Notes: swap bracketed placeholders with live metrics, wallet IDs, PNL,
        and day counts. Charts are intentionally illustrative and keep the Nunchi
        institutional aesthetic.
      </p>
    </footer>
  );
}
